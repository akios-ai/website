# Concepts Fondamentaux

AKIOS repose sur une philosophie de "Trust Signal" (Signal de Confiance) : la sécurité doit être intrinsèque, vérifiable et minimale. Ce n'est pas simplement un autre framework d'agents ; c'est une **cage de sécurité** pour l'IA.

## La Cage de Sécurité

Chaque flux de travail AKIOS s'exécute dans un environnement renforcé conçu pour empêcher les fuites de données, les accès non autorisés et les coûts incontrôlés.

### 1. Sandboxing (Isolation)
- **Linux Natif** : Utilise `seccomp-bpf` et `cgroups` v2 au niveau du noyau pour restreindre les appels système et l'utilisation des ressources.
- **Docker** : Utilise des politiques de conteneurs strictes, l'isolation réseau et des systèmes de fichiers racine en lecture seule.
- **Garantie** : Les agents ne peuvent pas s'échapper de la cage ni accéder au système hôte sans permission explicite.

### 2. Rédaction PII (Données Personnelles)
- **Protection Automatique** : Toutes les données entrant ou sortant de la cage sont analysées pour détecter les Informations Personnelles Identifiables (PII).
- **50+ Modèles** : Détecte les emails, numéros de téléphone, cartes de crédit, numéros de sécurité sociale, adresses IP, et plus encore.
- **Stratégies** :
  - `mask` : Remplace les données par `[REDACTED]` (Défaut).
  - `hash` : Remplace les données par un hachage cohérent pour la corrélation sans révéler les valeurs.
  - `remove` : Supprime entièrement les données.

### 3. Journal d'Audit Merkle
- **Inaltérable** : Chaque action (lecture de fichier, requête réseau, appel LLM) est enregistrée.
- **Chaîne Cryptographique** : Chaque entrée du journal contient un hachage SHA-256 de l'entrée précédente, formant une chaîne de Merkle.
- **Vérifiable** : Vous pouvez prouver mathématiquement que le journal d'audit n'a pas été altéré.

### 4. Coupe-Circuits de Coûts
- **Application du Budget** : Définissez une limite stricte (par ex. 1,00 $) par exécution de flux de travail.
- **Limites de Tokens** : Plafonnez le nombre de tokens par appel LLM.
- **Protection contre les Boucles** : Termine automatiquement les boucles infinies ou les processus incontrôlés.

## Les 4 Agents Principaux

AKIOS fournit quatre agents spécialisés et sécurisés. Vous n'écrivez pas de code arbitraire ; vous composez des flux de travail en utilisant ces primitives sûres.

### 📁 Agent Système de Fichiers (Filesystem)
- **But** : Lecture et écriture sécurisées de fichiers.
- **Sécurité** : Restreint aux `allowed_paths` spécifiques. Ne peut pas accéder aux fichiers système (`/etc`, `/var`) ou aux répertoires parents (`..`).
- **Actions** : `read`, `write`, `list`, `exists`, `stat`.

### 🌐 Agent HTTP
- **But** : Communication sécurisée avec des API externes.
- **Sécurité** : Limité en débit, délais d'attente imposés et soumis à la rédaction PII.
- **Actions** : `get`, `post`, `put`, `delete`.

### 🤖 Agent LLM
- **But** : Intelligence et raisonnement.
- **Sécurité** : Suivi des tokens, coût limité et rédaction PII. Les clés ne sont jamais enregistrées.
- **Fournisseurs** : OpenAI, Anthropic, Grok, Mistral, Gemini.
- **Actions** : `complete` (génération de texte), `chat` (conversationnel).

### 🛠️ Agent Exécuteur d'Outils (Tool Executor)
- **But** : Exécution d'outils en ligne de commande externes.
- **Sécurité** : Liste blanche de commandes stricte. S'exécute dans un sous-processus avec des privilèges réduits.
- **Actions** : `run`.

## Architecture

AKIOS est un environnement d'exécution **mono-processus**.
- **Pas de Sidecars** : Pas de base de données, pas de Redis, pas de services d'arrière-plan requis.
- **État** : Éphémère. L'état n'existe que pendant l'exécution du flux de travail.
- **Persistance** : Basée sur des fichiers (Entrées/Sorties) et Journaux d'Audit.
- **Déploiement** : Binaire unique, paquet Python ou conteneur Docker.

Cette architecture minimaliste garantit qu'AKIOS est **auditable par conception** et facile à déployer dans des environnements isolés (air-gapped) ou de haute sécurité.
